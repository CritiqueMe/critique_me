= semantic_form_for @questionnaire, :url => url do |f|
  = f.inputs do
    = f.input :name
    .clearer

  = f.actions do
    - if @questionnaire.new_record?
      = f.action :submit, :label => "Create Questionnaire"
    - else
      = f.action :submit, :label => "Edit Questionnaire"
